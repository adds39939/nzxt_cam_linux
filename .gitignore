*.log
/build/
/build-dir/
/repo/
/wine-*/
/.flatpak-builder/
*.flatpak
tests/test_descriptor_parse

# Left over from the native install this project used to ship. Nothing builds
# into it any more -- the Flatpak manifest builds Wine itself.
/prebuilt/
